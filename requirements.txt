# PyTorch is pre-installed in the RunPod base image (runpod/pytorch)
# HuggingFace ecosystem
transformers>=4.40.0
huggingface_hub>=0.23.0
# accelerate for multi-GPU / device placement helpers
accelerate>=0.30.0
# datasets for tokenizer utilities
datasets>=2.19.0

# Inference server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# Utilities
requests>=2.31.0
